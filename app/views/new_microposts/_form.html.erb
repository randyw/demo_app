<%= form_for(@new_micropost) do |f| %>
  <% if @new_micropost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@new_micropost.errors.count, "error") %> prohibited this new_micropost from being saved:</h2>

      <ul>
      <% @new_micropost.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_field :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
